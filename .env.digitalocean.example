# ============================================
# Trust Gambit - Digital Ocean Environment Variables
# ============================================

# ⚠️ IMPORTANT: Replace these values with your actual Digital Ocean setup!

# Database Configuration
# Get this from: Digital Ocean Dashboard → Databases → Your Database → Connection Details
DATABASE_URL=postgresql://doadmin:YOUR_PASSWORD@your-db-host.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# NextAuth Configuration
# Use your actual Digital Ocean app URL
NEXTAUTH_URL=https://your-app-name.ondigitalocean.app
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=REPLACE_WITH_GENERATED_SECRET
AUTH_TRUST_HOST=true

# Node Environment
NODE_ENV=production

# ============================================
# Setup Instructions for Digital Ocean
# ============================================

# OPTION 1: Using App Platform (Serverless - Recommended)
# --------------------------------------------------------
# 1. Create PostgreSQL Database:
#    Dashboard → Databases → Create → PostgreSQL 15
#    Copy connection string after creation
#
# 2. Create App:
#    Dashboard → Apps → Create App → GitHub
#    Select your trust-gambit repository
#
# 3. Add Environment Variables:
#    Your App → Settings → App-Level Environment Variables
#    Add all variables from this file
#
# 4. Connect Database:
#    Settings → Database → Add your PostgreSQL database
#    This adds DATABASE_URL automatically
#
# 5. Deploy!
#    App will auto-deploy on git push

# OPTION 2: Using Droplet (VM with Docker)
# --------------------------------------------------------
# 1. Create Droplet:
#    Ubuntu 22.04, $12/month or higher
#
# 2. SSH and install Docker:
#    ssh root@your-droplet-ip
#    curl -fsSL https://get.docker.com | sh
#    apt-get install docker-compose
#
# 3. Clone repo:
#    git clone https://github.com/koderAP/trust-gambit.git
#    cd trust-gambit
#
# 4. Configure environment:
#    cp .env.docker.example .env
#    nano .env
#    # Update DATABASE_URL to: postgresql://trustgambit:changeme123@postgres:5432/trustgambit
#    # Update NEXTAUTH_URL to: http://your-droplet-ip:3000
#
# 5. Deploy:
#    docker-compose up -d
#
# 6. Access at:
#    http://your-droplet-ip:3000

# ============================================
# Database Connection Examples
# ============================================

# Digital Ocean Managed Database (with SSL):
# DATABASE_URL=postgresql://doadmin:password@db-postgresql-xxx.db.ondigitalocean.com:25060/defaultdb?sslmode=require

# Docker Compose (internal network):
# DATABASE_URL=postgresql://trustgambit:changeme123@postgres:5432/trustgambit

# Local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/trustgambit

# ============================================
# Admin Credentials
# ============================================
# Auto-created on first app startup:
#   Username: admin
#   Password: admin@123
#
# ⚠️ Change password after first login!
# ============================================
